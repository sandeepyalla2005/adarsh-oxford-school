server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # Optional: proxy to backend if needed, 
    # but the portals are configured to hit the backend URL directly via environment variables
}
